'use strict';

var detextContext = require('../assets/js/detect-context.js');

describe('<Context detection>', function() {
  var fakeContext = 'Léo Hua--Döl';

  it('should recognize supported sites URLs', function() {

  });

  it('should return false for non-supported sites', function() {

  });

  it('should extract context query string on supported sites', function() {

  });
});
